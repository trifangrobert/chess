import classes from "./PawnPromotionBox.module.css";

const PawnPromotionBox = () => {
    return (  
        <div className={classes.container}>aici vor fi piese</div>

    );
}
 
export default PawnPromotionBox;